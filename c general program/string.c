#include<stdio.h>
#include  <string.h>
int main()
{    int a;
    char str[]="shivam";
    char str1[]={'q','a','t','y','a','m','\0'};
    char str2[54];
    // puts(str);
    // puts(strcat(str,str1));
    // printf("the length of str is %d \n",strlen(str));
    // printf("the length of str1 is %d \n",strlen(str1));
    // puts(strrev(str1));
    // strcpy(str2,str1);
    // puts(str2);
      printf("%d",strcmp(str,str1));

    return 0;
}
