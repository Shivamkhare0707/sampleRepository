#include<stdio.h>
#include<math.h>//whenever we are using pow fun we have to include math.h header file

int main()
{
    int  x=5,y=2,sum;
     sum=(pow(x,y));//syntex=pow(variable 1,variable 2)
     printf("the value of the x pow y is %f",sum);
    return 0;
}
